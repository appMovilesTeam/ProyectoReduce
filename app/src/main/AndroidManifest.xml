<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="salgadomontoya.anasofia.myreduce">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal_Activity"></activity>
        <activity android:name=".Inicio_Google" />
        <activity android:name=".PaginaPrincipal" />
        <activity android:name=".HomeBlog" />
        <activity android:name=".Detalle_articulo" />
        <activity android:name=".Datos" />
        <activity android:name=".Crear_cuenta" />
        <activity android:name=".crear_cuenta" />
        <activity android:name=".activity_crear_cuenta" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>